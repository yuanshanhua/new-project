# Compatible with prek (preferred) and pre-commit.
# Install: uv add --dev prek && uv run prek install
repos:
  - repo: local
    hooks:
      - id: check
        name: just check
        entry: just check
        language: system
        pass_filenames: false
        stages: [pre-commit]
